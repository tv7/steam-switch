# SteamSwitch is a Tauri (Rust) app + a Python sidecar (server.py).
#
# RUNTIME: none. The sidecar (server.py) and core/* are pure Python standard
# library — nothing to pip install to run from source.
#
# BUILD-ONLY (only needed to produce the distributable, see build.py /
# src-tauri/README.md):
#
#   pip install -r requirements.txt
#
# PyInstaller bundles the sidecar into server.exe.
PyInstaller>=6.0

# Optional, build-only: Pillow is used by assets/make_icon.py to (re)draw the
# source icon art. Not needed for normal builds — `cargo tauri icon` generates the
# app icons from the existing assets/steamswitch.png.
Pillow>=9.0
